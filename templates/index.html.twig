{% extends 'base.html.twig' %}

{% block title %}
    Bienvenue
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ parent() }}
{% endblock %}

{% block content_title %}
    Nouvelle ann&eacute;e scolaire
{% endblock %}

{% block content %}
    <form>
        <div class="form-group row">
            <label for="school_year_field" class="col col-form-label">Choisir une ann&eacute;e scolaire :</label>
            <div class="col">
                <select id="school_year_field" class="form-control">
                    {% for year in years %}
                        <option value="{{ year.idAnnee }}">{{ year.value }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
        <div id="school_classes">
            {% for index, class in classes %}
                <div class="form-group row">
                    <label for="class" class="col col-form-label">Classe {{ index + 1 }}</label>
                    <div class="col">
                        <input id="class" class="form-control" type="text" value="{{ class.name }}"/>
                    </div>
                    <label for="teacher" class="col col-form-label">Enseignant {{ index + 1 }}</label>
                    <div class="col">
                        <input id="teacher" class="form-control" type="text" value="{{ teacher.title }} {{ teacher.firstName }} {{ teacher.lastName }}"/>
                    </div>
                </div>
            {% endfor %}
        </div>
    </form>
{% endblock %}
